name: Prettier

on:
    push:

jobs:
    prettier:
        name: prettier
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - uses: oven-sh/setup-bun@v1
            - run: bun install
            - run: make format
